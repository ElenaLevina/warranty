# Передача файлов на ПК (локальная сеть)

После закрытия сессии (а также по мере съёмки) файлы кейса отправляются на
компьютер в локальной сети — вариант **A: HTTP-приёмник на ПК** (`server/`).

## Поток

1. После каждого снимка файл попадает в очередь (`StorageIndex`) и, если передача
   включена и есть сеть, сразу отправляется: на телефоне он **расшифровывается**
   (`CryptoService.openFile` → временный файл) и грузится на ПК; temp удаляется.
2. Нет сети / выключено → остаётся в очереди со статусом `pending`. При возврате
   сети (`NetworkUploadSync` через NetInfo) или на старте — `processQueue()` догружает.
3. По «ЗАКОНЧИЛ»: догрузка остатка + `POST …/complete` с `session.json` (это же
   закрывает §7 — уведомление рабочей станции). Сбой не блокирует закрытие — очередь
   повторит позже.
4. Индикаторы-облака в активной сессии показывают статус: ожидает / загрузка /
   загружено / ошибка.

Файлы на телефоне зашифрованы (Keystore), ключ не покидает устройство, поэтому
расшифровка делается **на телефоне перед отправкой** → на ПК лежат читаемые файлы.

## REST-контракт

Базовый адрес — `http://<IP-ПК>:<порт>` (вводится в **Настройки** приложения).
Все запросы: заголовок `Authorization: Bearer <token>`.

| Метод | Путь | Тело | Назначение |
|---|---|---|---|
| `GET`  | `/v1/health` | — | Проверка доступности (кнопка «Проверить соединение») |
| `POST` | `/v1/cases/{caseId}/files` | multipart: `file` + поля `filename`, `type` (`photo`/`video`), `size` | Один файл кейса |
| `POST` | `/v1/cases/{caseId}/complete` | `session.json` (JSON) | Кейс завершён |

- Идемпотентность: если файл уже сохранён — `200`, пропуск (безопасные повторы).
- **Целостность по размеру:** клиент шлёт `size` (надёжно через `RNFS.stat`),
  сервер сверяет число принятых байт; несовпадение → `400` (недокачка, повтор).
  `RNFS.hash` не используется — он расходился с реально отправленными байтами.

## TLS (опционально)

По умолчанию — bearer-токен по HTTP в доверенной LAN. Для шифрования канала:

1. На ПК сгенерировать самоподписанный сертификат с IP компьютера в SAN:
   ```bash
   server/scripts/gen-cert.sh 10.0.0.9      # → cert.pem (CA) + key.pem
   ```
2. Запустить приёмник по HTTPS:
   ```bash
   ./warranty-receiver -dir ... -token ... -addr :8443 -cert cert.pem -key key.pem
   ```
3. Установить `cert.pem` на телефон как **сертификат ЦС** (Android: Настройки →
   Безопасность → Зашифровать и учётные данные → Установить сертификат → Сертификат ЦС).
4. В приложении указать адрес `https://10.0.0.9:8443`.

Приложение доверяет пользовательским CA (`android/app/src/main/res/xml/network_security_config.xml`),
поэтому самоподписанный сертификат после установки принимается. Cleartext-HTTP
остаётся разрешён (можно работать и без TLS).

## Где что в коде

| Слой | Файл |
|---|---|
| Конфиг (адрес/токен/вкл) | `src/services/upload/uploadConfig.ts` |
| Контракт транспорта | `src/services/upload/uploadTransport.ts` |
| Реальный транспорт (RNFS+fetch) | `src/services/upload/rnfsHttpTransport.ts` |
| Сервис загрузки/очередь | `src/services/upload/httpUploadService.ts` |
| Экран настроек | `src/screens/SettingsScreen.tsx` |
| Авто-докачка по сети | `src/app/NetworkUploadSync.tsx` |
| Сервер на ПК | `server/` (Go) |

## Настройка (коротко)

1. На ПК собрать и запустить `server/` (`-dir`, `-token`, `-addr`), открыть порт.
2. В приложении: **Старт → ⚙ Настройки** → включить передачу, ввести `http://<IP>:<порт>`
   и токен → «Проверить соединение» → «Сохранить».
