# Индекс документации — Warranty

Карта документов проекта. Обновлять при добавлении/изменении docs.

| Документ | Назначение |
|---|---|
| [architecture.md](./architecture.md) | Архитектура: сервисы за интерфейсами, потоки данных, лайфцикл сессии, READ ONLY инвариант |
| [native-modules.md](./native-modules.md) | Нативные модули Фаз 6–7 (ML Kit OCR, Keystore), контракты и сборка на устройстве |
| [upload.md](./upload.md) | Передача файлов на ПК в LAN: поток, REST-контракт, сервер `server/`, экран настроек |
| [device-testing.md](./device-testing.md) | Фаза 8: реальная камера + OCR на телефоне — включение флагов, сборка, чек-лист теста |
| [../CLAUDE.md](../CLAUDE.md) | ТЗ-контекст модуля (источник истины по требованиям) |
| [../SETUP.md](../SETUP.md) | Настройка окружения разработки (Android, JDK, устройство для OCR) |
| [../.claude/skills/warranty-module/SKILL.md](../.claude/skills/warranty-module/SKILL.md) | Детальные правила: форматы номеров, session.json, лайфцикл, офлайн |

## Статус реализации (итерация 1 — скелет + вертикальный срез)

| Фаза | Описание | Статус |
|---|---|---|
| 0 | Инициализация RN-проекта, инструментарий, структура | ✅ готово |
| 1 | Типы и доменная модель | ✅ готово |
| 2 | Парсер израильских номеров (TDD) | ✅ готово |
| 3 | Сервисы за интерфейсами (mock/stub) | ✅ готово |
| 4 | Лайфцикл сессии + store (TDD) | ✅ готово |
| 5 | Навигация + 4 экрана (вертикальный срез) | ✅ готово |
| 6 | Реальный нативный мост ML Kit (на устройстве) | 🧩 скелет готов, собирать на устройстве |
| 7 | Шифрование at-rest (Android Keystore, на устройстве) | ✅ включено (FEATURES.nativeCrypto=true), проверять на устройстве |

### Авторизация (§8) — готово

Локальный вход без бэкенда: регистрация при первом запуске (логин + PIN), PIN-разблокировка
на каждом запуске, «Сменить механика». `mechanic_id` берётся из auth; список сессий
изолируется по механику (`FilesService.listOpenSessions(mechanicId)`). PIN — salted SHA-256
в зашифрованном MMKV. Биометрия — за интерфейсом `AuthService`, отдельным шагом.
Файлы: `src/services/auth/*`, `src/store/authStore.ts`, `src/screens/{Register,Lock}Screen.tsx`,
гейт в `RootNavigator`.

### Проверки среза (Фазы 0–5)

`npm test` — 37 тестов зелёные · `npm run typecheck` (tsc) — чисто · `npm run lint` — 0 ошибок.
Тесты ядра (парсер, FilesService, store, экраны) выполняются на Node без устройства.
Сборку APK/эмулятор и Фазы 6–7 проверять в среде с Android SDK и физическим телефоном
(см. `SETUP.md`).

## Вне скоупа итерации

Реальный бэкенд/загрузка/уведомления станции, серверный auth, ручной ввод номера —
см. раздел «Out of scope» плана и `CLAUDE.md §9`.
